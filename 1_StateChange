const int led=2;
int contador=0;
void setup()
{
  Serial.begin(9600);
  pinMode(2, OUTPUT);
  attachInterrupt(digitalPinToInterrupt(2),avisar,CHANGE);
}

void loop(){
  
}
void avisar(){
 Serial.println("cambio de estado"); 
}
